# examples

LangGraph examples for the Countersign wire format and resume helpers.

The [`refund-agent`](refund-agent) example pauses a real graph before a fake
refund, exercises compatible approve/edit/reject responses, probes LangGraph
Platform request shapes, and verifies durable Postgres checkpoint semantics.
